{
  "name": "elsewhere-updates",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check src/app.js && node --check src/model.js && node --check scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "collect": "python scripts/collect.py"
  },
  "engines": {
    "node": ">=22"
  }
}
